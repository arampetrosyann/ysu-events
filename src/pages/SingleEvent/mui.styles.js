import { makeStyles } from "@material-ui/styles";

const useStyles = makeStyles({
  buttonDelete: {
    color: "#740f0f",
    fontSize: 22,
  },
  buttonEdit: {
    color: "#1C3398",
    fontSize: 22,
  },
  buttonDone: {
    color: "#0B9618",
    fontSize: 22,
  },
});

export default useStyles;
